<div id="blog-banner" class="sub-banner text-center">
  <div class="filter">
    <div class="row">
      <div class="col-xs-12 center">
        <div class="vertical-spacer"></div>
        <h1><%= t "press.title" %></h1>
        <div class="vertical-spacer"></div>
      </div>
    </div>
  </div>
</div>

<div class="vertical-spacer"></div>

<div class="container">
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <% ARTICLES.each_with_index do | article, index| %>
        <div class="col-md-12 data-article">
          <div class="col-md-2">
              <a class="title" href="<%= article[:url]%>"><img class="logo" src="<%= image_path "medias/#{article[:logo]}" %>" alt="<%= article[:name] %>"></a>
          </div>
          <div class="col-md-10 press-article">
            <a class="title" href="<%= article[:url]%>"><%= article[:name]%></a></p>
            <p class="published"><%= article[:date].strftime("%d %b %Y") %> - <%= article[:media] %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>

<div class="vertical-spacer"></div>

<%= erb :share %>